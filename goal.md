# 一个vscode plugin项目，目标是开发一个基于正则表达式的文本文件viewer
## 运行环境
1. 插件主要基于vscode默认的编辑器进行功能强化，不允许重新自定义一个editor（如果确实需要，尽量兼容默认editor的常规功能）
## 功能概述
1. 每个打开的editor tab, 附带一个配置面板，这个配置面板可以增/删/改 多个正则表达式组，并且可以配置这个正则表达式组匹配行的文本颜色
2. 每个正则表达式组可以配置多个正则表达式，可以配置这些表达式的逻辑关系，包括：and, or, not(与，或，非)
3. editor每一行满足某个正则表达式组包含的所有正则表达式满足所有规则就会默认以知道文本颜色显示，否则默认自动折叠
4. 如果某一行同时满足多个正则表达式组，那以首先满足的组为准，后续匹配可以排除已经被匹配的行
5. 配置不需要实现生效，配置面板提供一个“go“按键，用户按下后再生效

## 代码要求
1. 架构按mvc分层，定义清晰，方便人类阅读的接口进行层级解耦
2. 注释要简洁，易懂
3. 开始代码编写前需要启动一个顶级架构师角色sub agent完成“概要设计”，“详细设计”，“接口设计”，“原型设计”等文档，写到dev_doc目录下，针对每个文档，设定一个顶级review的角色启动sub agent对文档进行review，反复讨论修改后（最多10轮）提交最终版本的“概要设计”，“详细设计”，“接口设计”，“原型设计”等文档
4. 主agent基于最终文档，启动一个sub agent进行接口代码开发，启动一个sub agent对接口代码进行review,确保满足设计文档要求
5. 接口开发完成后，主agent按功能生成多个开发sub agent对项目进行并行开发，每个开发sub agent配备一个review sub agent来确保代码质量和功能完整，测试等工作
6. 另设一个sub agent，专门针对功能设计全自动化的测试流程和用例。每个功能开发完后，必须能通过这个测试sub agent的验收
7. 开发过程自创建git仓库，开发的过程中每完成一项任务或者修复完成一个问题，必须提交代码再进行review
8. 代码由sub agent提交，如果出现合并冲突，由主agent解决冲突，期间所有sub agent不允许提交代码，主agent解决完冲突后通知恢复提交代码权限

## 风格要求
1. 设计和代码功能要收敛，不能太发散，该简单实现的功能不要过度设计